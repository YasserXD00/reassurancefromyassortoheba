# React App

This is a React application built with Vite.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Configure your environment variables in `.env.local` (see `.env.example` for reference)
3. Run the app:
   `npm run dev`
